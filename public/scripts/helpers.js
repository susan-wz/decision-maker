// function to create new option section... trying to put in client.js and it doesn't work :()

/*
const createOptionElement = function () {
  const $option = $("<div>").addClass("option");
  const optionBlock = `<p>Option Three</p>
  <input type="text" name="option" />
  <p>Option Three Description (Optional)</p>
  <input type="text" name="option" required /><i class="far fa-trash-alt"></i>`
  $option.append(optionBlock);
  return $option;
}

const renderOptionElement = function () {
  $('#add-options').on('click', function () {
    $("choices").load("/test.html #option")
  }
  )
}
*/